{
  "name": "snarf-all-the-things",
  "version": "1.0.0",
  "description": "A Chrome browser extension to grab image source URLs and bundle them into a single text file.",
  "main": "popup.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kenson Johnson",
  "license": "MIT",
  "dependencies": {
    "chrome-types": "^0.1.206"
  }
}
